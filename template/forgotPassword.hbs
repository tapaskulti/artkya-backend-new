{{!-- ==================== FORGOT PASSWORD TEMPLATE (templates/emails/forgot-password.hbs) ==================== --}}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reset Your Password - Artkya</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            line-height: 1.6;
            color: #2c3e50;
            background-color: #f8f9fa;
        }
        
        .container {
            max-width: 600px;
            margin: 0 auto;
            background: white;
            box-shadow: 0 0 20px rgba(0,0,0,0.1);
        }
        
        .header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 40px 30px;
            text-align: center;
            position: relative;
            overflow: hidden;
        }
        
        .header::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, rgba(255,255,255,0.1) 0%, transparent 70%);
            animation: shimmer 3s infinite alternate;
        }
        
        @keyframes shimmer {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
        
        .logo {
            font-size: 36px;
            font-weight: bold;
            margin-bottom: 15px;
            letter-spacing: 3px;
            position: relative;
            z-index: 1;
        }
        
        .lock-icon {
            font-size: 48px;
            margin: 20px 0;
            position: relative;
            z-index: 1;
        }
        
        .header-title {
            font-size: 24px;
            font-weight: 600;
            position: relative;
            z-index: 1;
        }
        
        .content {
            padding: 50px 30px;
        }
        
        .greeting {
            font-size: 24px;
            color: #2c3e50;
            margin-bottom: 25px;
            font-weight: 600;
        }
        
        .intro-text {
            font-size: 16px;
            color: #5a6c7d;
            margin-bottom: 30px;
            line-height: 1.8;
        }
        
        .reset-section {
            background: linear-gradient(135deg, #74b9ff 0%, #0984e3 100%);
            color: white;
            padding: 40px 30px;
            border-radius: 16px;
            text-align: center;
            margin: 40px 0;
            position: relative;
            overflow: hidden;
        }
        
        .reset-section::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
            animation: slide 2s infinite;
        }
        
        @keyframes slide {
            0% { left: -100%; }
            100% { left: 100%; }
        }
        
        .reset-title {
            font-size: 22px;
            margin-bottom: 15px;
            font-weight: 600;
            position: relative;
            z-index: 1;
        }
        
        .reset-text {
            font-size: 16px;
            margin-bottom: 30px;
            opacity: 0.95;
            position: relative;
            z-index: 1;
        }
        
        .reset-button {
            display: inline-block;
            background: white;
            color: #0984e3;
            padding: 18px 40px;
            text-decoration: none;
            border-radius: 50px;
            font-weight: 600;
            letter-spacing: 1px;
            transition: all 0.3s ease;
            position: relative;
            z-index: 1;
            box-shadow: 0 4px 15px rgba(0,0,0,0.2);
        }
        
        .reset-button:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(0,0,0,0.3);
            color: #0984e3;
        }
        
        .token-info {
            background: #fff3cd;
            border: 1px solid #ffeaa7;
            border-radius: 12px;
            padding: 25px;
            margin: 30px 0;
        }
        
        .token-title {
            font-size: 18px;
            color: #856404;
            margin-bottom: 15px;
            font-weight: 600;
            display: flex;
            align-items: center;
        }
        
        .token-title::before {
            content: "⚠️";
            margin-right: 10px;
            font-size: 20px;
        }
        
        .token-text {
            color: #856404;
            margin-bottom: 15px;
        }
        
        .expiry-time {
            font-size: 18px;
            font-weight: bold;
            color: #dc3545;
            text-align: center;
            padding: 15px;
            background: #f8d7da;
            border-radius: 8px;
            margin-top: 15px;
        }
        
        .manual-reset {
            background: #f8f9fa;
            border-radius: 12px;
            padding: 25px;
            margin: 30px 0;
            border-left: 4px solid #6c757d;
        }
        
        .manual-title {
            font-size: 18px;
            color: #2c3e50;
            margin-bottom: 15px;
            font-weight: 600;
        }
        
        .manual-text {
            color: #5a6c7d;
            margin-bottom: 15px;
        }
        
        .manual-link {
            font-family: 'Courier New', monospace;
            background: #e9ecef;
            padding: 10px;
            border-radius: 4px;
            word-break: break-all;
            font-size: 14px;
            color: #495057;
        }
        
        .security-section {
            background: #e8f4fd;
            padding: 25px;
            border-radius: 12px;
            margin: 30px 0;
        }
        
        .security-title {
            font-size: 18px;
            color: #2c3e50;
            margin-bottom: 15px;
            font-weight: 600;
            display: flex;
            align-items: center;
        }
        
        .security-title::before {
            content: "🔒";
            margin-right: 10px;
            font-size: 20px;
        }
        
        .security-list {
            list-style: none;
            padding: 0;
        }
        
        .security-item {
            padding: 8px 0;
            color: #2c3e50;
            position: relative;
            padding-left: 25px;
        }
        
        .security-item:before {
            content: "•";
            position: absolute;
            left: 0;
            color: #0984e3;
            font-weight: bold;
            font-size: 18px;
        }
        
        .help-section {
            text-align: center;
            margin: 40px 0;
            padding: 30px;
            background: linear-gradient(135deg, #e8f5e8 0%, #d4edda 100%);
            border-radius: 12px;
        }
        
        .help-title {
            font-size: 20px;
            color: #155724;
            margin-bottom: 15px;
            font-weight: 600;
        }
        
        .help-text {
            color: #155724;
            margin-bottom: 20px;
        }
        
        .help-button {
            display: inline-block;
            background: #28a745;
            color: white;
            padding: 12px 30px;
            text-decoration: none;
            border-radius: 25px;
            font-weight: 600;
            transition: all 0.3s ease;
        }
        
        .help-button:hover {
            background: #218838;
            transform: translateY(-1px);
        }
        
        .footer {
            background: #2c3e50;
            color: white;
            padding: 40px 30px;
            text-align: center;
        }
        
        .footer-content {
            margin-bottom: 25px;
        }
        
        .support-text {
            font-size: 16px;
            margin-bottom: 10px;
        }
        
        .support-email {
            color: #3498db;
            text-decoration: none;
            font-weight: 600;
        }
        
        .footer-bottom {
            padding-top: 25px;
            border-top: 1px solid #34495e;
            font-size: 14px;
            color: #95a5a6;
        }
        
        .disclaimer {
            background: #f8f9fa;
            border: 1px solid #dee2e6;
            border-radius: 8px;
            padding: 20px;
            margin: 30px 0;
            font-size: 14px;
            color: #6c757d;
        }
        
        @media (max-width: 600px) {
            .container {
                margin: 0;
                box-shadow: none;
            }
            
            .header, .content, .footer {
                padding: 30px 20px;
            }
            
            .reset-section {
                padding: 30px 20px;
            }
            
            .greeting {
                font-size: 22px;
            }
            
            .reset-button {
                padding: 15px 30px;
                font-size: 16px;
            }
            
            .manual-link {
                font-size: 12px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Header -->
        <div class="header">
            <div class="logo">ARTKYA</div>
            <div class="lock-icon">🔐</div>
            <div class="header-title">Password Reset Request</div>
        </div>

        <!-- Main Content -->
        <div class="content">
            <div class="greeting">Hello {{user.firstName}}!</div>
            
            <p class="intro-text">
                We received a request to reset your password for your Artkya account. If you made this request, click the button below to create a new password. If you didn't request this, please ignore this email.
            </p>

            <!-- Reset Password Section -->
            <div class="reset-section">
                <div class="reset-title">Reset Your Password</div>
                <p class="reset-text">Click the button below to securely reset your password</p>
                <a href="{{resetPasswordUrl}}" class="reset-button">Reset My Password</a>
            </div>

            <!-- Token Information -->
            <div class="token-info">
                <div class="token-title">Important Security Information</div>
                <p class="token-text">
                    This password reset link is valid for a limited time and can only be used once. For your security, it will expire automatically.
                </p>
                <div class="expiry-time">
                    ⏰ This link expires in {{expiryTime}} minutes
                </div>
            </div>

            <!-- Manual Reset Option -->
            <div class="manual-reset">
                <div class="manual-title">Can't click the button?</div>
                <p class="manual-text">Copy and paste this link into your browser:</p>
                <div class="manual-link">{{resetPasswordUrl}}</div>
            </div>

            <!-- Security Tips -->
            <div class="security-section">
                <div class="security-title">Security Best Practices</div>
                <ul class="security-list">
                    <li class="security-item">Choose a strong password with at least 8 characters</li>
                    <li class="security-item">Include uppercase, lowercase, numbers, and special characters</li>
                    <li class="security-item">Don't reuse passwords from other accounts</li>
                    <li class="security-item">Consider using a password manager</li>
                    <li class="security-item">Enable two-factor authentication if available</li>
                </ul>
            </div>

            <!-- Help Section -->
            <div class="help-section">
                <div class="help-title">Need Help?</div>
                <p class="help-text">
                    If you're having trouble resetting your password or didn't request this change, our support team is here to help.
                </p>
                <a href="mailto:{{supportEmail}}" class="help-button">Contact Support</a>
            </div>

            <!-- Disclaimer -->
            <div class="disclaimer">
                <strong>Important:</strong> If you didn't request this password reset, please ignore this email. Your account remains secure, and no changes will be made unless you click the reset link above. For additional security concerns, please contact our support team immediately.
            </div>
        </div>

        <!-- Footer -->
        <div class="footer">
            <div class="footer-content">
                <div class="support-text">Questions or concerns?</div>
                <a href="mailto:{{supportEmail}}" class="support-email">{{supportEmail}}</a>
            </div>
            
            <div class="footer-bottom">
                <p>&copy; {{currentYear}} Artkya. All rights reserved.</p>
                <p>This email was sent to {{user.email}}. You received this because a password reset was requested for your account.</p>
            </div>
        </div>
    </div>
</body>
</html>