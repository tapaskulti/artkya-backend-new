{{!-- ==================== SIMPLE CUSTOMER CONFIRMATION TEMPLATE (templates/customer-order-confirmation.hbs) ==================== --}}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Inquiry Confirmation - Artkya</title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            background-color: #f3f4f6;
            margin: 0;
            padding: 20px;
        }
        .container {
            max-width: 600px;
            margin: 0 auto;
            background: white;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
            border-radius: 0.375rem;
            padding: 1.5rem;
        }
        .title {
            font-size: 1.5rem;
            font-weight: bold;
            color: #1f2937;
            margin-bottom: 1rem;
        }
        .subtitle {
            color: #374151;
            margin-bottom: 1.5rem;
        }
        .content {
            color: #374151;
            margin-bottom: 1.5rem;
        }
        .details-box {
            background-color: #f9fafb;
            padding: 1rem;
            border-radius: 0.375rem;
            border: 1px solid #e5e7eb;
        }
        .detail-item {
            font-size: 0.875rem;
            color: #4b5563;
            margin-bottom: 0.5rem;
        }
        .detail-item:last-child {
            margin-bottom: 0;
        }
        .strong {
            font-weight: 600;
        }
        .footer-text {
            color: #374151;
            margin-top: 1.5rem;
        }
        .signature {
            color: #374151;
            margin-top: 1rem;
        }
        .company-info {
            margin-top: 1.5rem;
            font-size: 0.875rem;
            color: #6b7280;
        }
        .link {
            color: #3b82f6;
            text-decoration: none;
        }
        .link:hover {
            text-decoration: underline;
        }
        .artwork-section {
            background-color: #f0f9ff;
            padding: 1rem;
            border-radius: 0.375rem;
            border: 1px solid #bae6fd;
            margin: 1rem 0;
        }
        .artwork-title {
            font-weight: 600;
            color: #0c4a6e;
            margin-bottom: 0.5rem;
        }
        .artwork-image {
            max-width: 200px;
            height: 150px;
            object-fit: cover;
            border-radius: 0.375rem;
            margin: 0.5rem 0;
        }
        .success-banner {
            background-color: #d1fae5;
            border: 1px solid #10b981;
            padding: 1rem;
            border-radius: 0.375rem;
            margin-bottom: 1.5rem;
            text-align: center;
        }
        .success-text {
            color: #047857;
            font-weight: 600;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="success-banner">
            <div class="success-text">✅ Your inquiry has been successfully received!</div>
        </div>

        <h1 class="title">Inquiry Confirmation</h1>
        
        <p class="subtitle">Hi <span class="strong">{{customer.firstName}}</span>,</p>
        
        <p class="content">
            Thank you for your interest in <strong>Artkya</strong>! We have received your inquiry and our team will contact you soon. Below are your inquiry details:
        </p>

        <div class="details-box">
            <p class="detail-item"><strong>Name:</strong> {{customer.fullName}}</p>
            <p class="detail-item"><strong>Email ID:</strong> {{customer.email}}</p>
            <p class="detail-item"><strong>Contact Number:</strong> {{customer.contactNumber}}</p>
            <p class="detail-item"><strong>Address:</strong><br>{{customer.address}}</p>
            {{#if customer.description}}
            <p class="detail-item"><strong>Your Notes:</strong><br>{{customer.description}}</p>
            {{/if}}
            <p class="detail-item"><strong>Inquiry Date:</strong> {{orderDate}} at {{orderTime}}</p>
        </div>

        {{#if artwork.title}}
        <div class="artwork-section">
            <div class="artwork-title">🎨 Artwork You're Interested In</div>
            {{#if artwork.image}}
            <img src="{{artwork.image}}" alt="{{artwork.title}}" class="artwork-image">
            {{/if}}
            <p class="detail-item"><strong>Title:</strong> {{artwork.title}}</p>
        </div>
        {{/if}}

        <p class="footer-text">
            Our team will contact you within 24 hours to discuss pricing, availability, and next steps. If you have any questions, just reply to this email.
        </p>

        <p class="signature">Thanks again,<br>The Artkya Team</p>
        
        <div class="company-info">
            <p>Artkya</p>
            <p><a href="{{websiteUrl}}" class="link">www.artkya.com</a> | {{supportEmail}}</p>
        </div>
    </div>
</body>
</html>